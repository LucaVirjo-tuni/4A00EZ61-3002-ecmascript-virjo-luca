let carBands = ['Citroen', 'Volvo', 'Ferrari', 'Mercedes', 'Renault', 'Jeep', 'Skoda']

for(let i = 0; i < carBands.length; i++) {
    console.log(carBands[i])
}

console.log()

for(let element of carBands) {
    console.log(element)
}